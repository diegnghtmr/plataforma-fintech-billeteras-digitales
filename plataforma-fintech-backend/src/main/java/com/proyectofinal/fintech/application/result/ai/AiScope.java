package com.proyectofinal.fintech.application.result.ai;

/**
 * Scope of the AI request — determines which data sources are included in the context.
 * Zero Spring imports.
 */
public enum AiScope {
    USER,
    ADMIN
}
